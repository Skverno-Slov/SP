<Window x:Class="LabWork17.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork17" xmlns:viewmodel="clr-namespace:LabWork17.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Меню">
                <MenuItem Header="Этот компьютер" Command="{Binding GoToMyComputerCommand}"/>
            </MenuItem>
        </Menu>

        <DockPanel>
            <TextBox Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top">
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding GoToExplorerCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <Frame Content="{Binding CurrentPage}" 
           NavigationUIVisibility="Hidden"/>
        </DockPanel>
    </DockPanel>
</Window>
