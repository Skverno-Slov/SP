using Terminal.Gui;

Application.Init();

using Window window = new() { Title = "Hello Nigger (Esc to quit)" };
Label label = new()
{
    Text = "Hello, Terminal.Gui v2!",
    X = Pos.Center(),
    Y = Pos.Center()
};
window.Add(label);

var 

Application.Run(window);